import React from 'react';

function sum(a,b) {
  return a + b;
}

function sub(a,b) {
  return a - b;
}

function divide(a,b) {
  return a / b;
}

function multiply(a,b) {
  return a * b;
}

export default {sum, sub, divide, multiply};

